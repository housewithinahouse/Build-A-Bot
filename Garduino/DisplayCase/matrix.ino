//int matrixCursorPos = 20; 
//unsigned long matrixTriggerTime = 0;

//#include <Adafruit_GFX.h>
//#include "Adafruit_LEDBackpack.h"
//
//
//Adafruit_8x8matrix matrix = Adafruit_8x8matrix();
//
//matrix.begin(0x70);  // pass in the address
//
//void LEDMatrixShow(){
//  matrix.setTextSize(1);
//  matrix.setTextWrap(false);  // we dont want text to wrap so it scrolls nicely
//  matrix.setTextColor(LED_ON);
//  
//  for (int8_t x=20; x>=-70; x--) { // this code needs to be rewritten so it doesn't inturupt the rest of the processees...
//    matrix.clear();
//    matrix.setCursor(x,0);
//    matrix.print("Build A Bot");
//    matrix.writeDisplay();
//  }
//}

//I don't know if we have enough memory to fit all of this in. We might just be out of room...lol. Never done that before! 
